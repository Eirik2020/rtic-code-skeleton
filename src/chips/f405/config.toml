chip = "f405"
cargo_feature = "f405"
pac_feature = "stm32f4xx-hal/stm32f405"
default_part = "STM32F405RGTx"

[parts.STM32F405RGTx]
probe_chip = "STM32F405RGTx"
flash_kib = 1024
# SRAM1 + SRAM2; CCM is a separate address region, not contiguous RAM.
ram_kib = 128
ccm_kib = 64
